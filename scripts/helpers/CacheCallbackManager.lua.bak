Mod = Torment

function Mod:CacheCallbackUpdater(player, cache_flag)
	print("i triggered")
	if cache_flag == 1152 then
		player.Luck = player.Luck + 1
	end
end

Mod:AddCallback(ModCallbacks.MC_EVALUATE_CACHE, Mod.CacheCallbackUpdater)